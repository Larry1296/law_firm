import { AlertCircle, CheckCircle2, Info } from 'lucide-react';

export function FormAlert({ variant = 'error', title, children, className = '' }) { const styles = { error: ['border-red-200 bg-red-50 text-red-900 dark:border-red-900 dark:bg-red-950/30 dark:text-red-200', AlertCircle], info: ['border-blue-200 bg-blue-50 text-blue-950 dark:border-blue-900 dark:bg-blue-950/30 dark:text-blue-200', Info], success: ['border-emerald-200 bg-emerald-50 text-emerald-950 dark:border-emerald-900 dark:bg-emerald-950/30 dark:text-emerald-200', CheckCircle2] }; const [style, Icon] = styles[variant] || styles.info; return <div role={variant === 'error' ? 'alert' : 'status'} className={`flex gap-3 rounded-lg border p-4 text-sm ${style} ${className}`}><Icon size={18} className='mt-0.5 shrink-0' /><div>{title && <p className='font-semibold'>{title}</p>}<div className={title ? 'mt-1' : ''}>{children}</div></div></div>; }
export function FormValidationSummary({ errors = {}, title = 'Please correct the highlighted fields.' }) { const items = Object.entries(errors).filter(([, value]) => value).map(([key, value]) => ({ key, value: typeof value === 'string' ? value : value?.message })).filter((item) => item.value); if (!items.length) return null; return <div className='form-validation-summary' role='alert' tabIndex={-1}><strong>{title}</strong><ul>{items.map((item) => <li key={item.key}>{item.value}</li>)}</ul></div>; }

