import Input from "@/components/ui/Inputs";

export const Input3D = (props) => {
  return (
    <div className='bg-transparent text-[color:var(--text-primary)]'>
      <Input {...props} />
    </div>
  );
};
